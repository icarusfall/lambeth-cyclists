[pytest]
# The processor's suite, plus the shared package's own. Collection is scoped
# so a bare `pytest` cannot pick up the helper scripts, which hit live APIs.
testpaths = processor/tests core/tests
